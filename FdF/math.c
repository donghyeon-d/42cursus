#include <stdio.h>
#include <math.h>

#define DEGREE 0.5

int main()
{
	// double x = 2;
	// double y = 3;
	// double x30 = x * cos(0.5) - y * sin(0.5);
	// double y30 = x * sin(0.5) + y * cos(0.5);
	// // double xr30 = (x * cos(0.5) - y * sin(0.5)) * -1;
	// // double yr30 = (x * sin(0.5) + y * cos(0.5)) * -1;
	// double xr30 = x * cos(0.5) + y * sin(0.5);
	// double yr30 = y * cos(0.5) - x * sin(0.5);
	// printf("%f %f\n", x, y);
	// printf("%f %f\n", x30, y30);
	// printf("%f %f\n", xr30, yr30);
	// 2, 3
	double a = 2.6;
	// int a =
	printf("%f", a - (int)a);
}